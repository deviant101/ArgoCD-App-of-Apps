apiVersion: apps/v1
kind: Deployment
metadata:
  name: app3-dashboard
  namespace: app3-dashboard
  labels:
    app: app3-dashboard
    version: "1.0.0"
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: app3-dashboard
  template:
    metadata:
      labels:
        app: app3-dashboard
        version: "1.0.0"
    spec:
      containers:
      - name: app3-dashboard
        image: "589736534170.dkr.ecr.us-east-1.amazonaws.com/app-of-apps:app3-dashboard-main-c8bbcf2-20260622084055"
        imagePullPolicy: IfNotPresent
        ports:
        - name: http
          containerPort: 8082
          protocol: TCP
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 10
          periodSeconds: 30
          timeoutSeconds: 3
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 3
          failureThreshold: 3
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          runAsNonRoot: true
          runAsUser: 101
          capabilities:
            drop:
            - ALL
